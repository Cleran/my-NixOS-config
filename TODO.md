# TODO

- nix-ld: run scripts and binaries that need dynamic libs
- project switcher: workflow improvement
    - automatically create and reattach zellij sessions
    - set appropriate layout
    - fuzzy find projects
    - automatically use the correct ssh-key
